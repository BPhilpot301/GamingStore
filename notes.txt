urls:
from django.urls import path
from . import views

urlpatterns = [
    path('home/', views.home_view, name="home"),
    path('about/', views.about_view, name="about"),
]

views
from django.shortcuts import render

# Create your views here.
def home_view(request):
    return render(request, 'pages/home.html')

def about_view(request):
    return render(request, 'pages/about.html')

    # creata virtual env:
python3 -m venv venv


# activate venv
win: venv\Scripts\activate
mac: source venv/bin/activate

# install django
python3 -m pip install django whitenoise pillow


# create the project
django-admin startproject config .

____________________________

# create a pages app
python3 manage.py startapp pages

everytime you modify any of the models you need to run migrations
python manage.py makemigrations
python manage.py migrate

even if you are just fixing a typo you still have to do these two commands


Class-based views:

View         = Generic View
ListView     = get a list of records
DetailView   = get a single (detail) record
CreateView   = create a new record
DeleteView   = remove a record
UpdateView   = modify an existing record
LoginView    = LogIn

"""




Add to base html
<a class="nav-link" href="{% url 'login' %}">Log In</a>
<a class="nav-link" href="{% url 'account' %}">Create Account</a>


Standard Pokémon Card size, 2.5 inches wide by 3.5 inches tall (63.5 mm by 88.9 mm). The thickness typically ranges from 0.01 to 0.03 inches.